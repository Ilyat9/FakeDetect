# Base URL of the FakeDetect FastAPI backend.
# Development: Vite dev-server proxies /api to this target (see vite.config.ts).
VITE_PROXY_TARGET=http://localhost:8000

# In dev the app calls the API on a relative /api/v1 path (proxied above).
VITE_API_URL=/api/v1
